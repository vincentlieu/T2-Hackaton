<%= form_with model: room, local: true do |form| %>
<div>
    <%= form.label :title %>
    <%= form.text_field :title, value: room.title %>
</div>
<div>
<%= form.submit "Create a Room", class: "button is-primary" %>
</div>
<% end %>